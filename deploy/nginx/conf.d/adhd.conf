upstream adhd_backend {
    server adhd-app-server:8080;
    keepalive 16;
}

server {
    listen 80;
    listen [::]:80;
    server_name api.xiaobuxing.cn;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name api.xiaobuxing.cn;

    ssl_certificate /etc/nginx/certs/api.xiaobuxing.cn_bundle.crt;
    ssl_certificate_key /etc/nginx/certs/api.xiaobuxing.cn.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    client_max_body_size 10m;

    location / {
        proxy_pass http://adhd_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_connect_timeout 10s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }
}
